<h1>Articles</h1>
<%= link_to "All Books", article_path %>

<ul>
  <% @articles.each do |article| %>
    <li>
      <%= link_to article.title, "/article/#{article.created_at.year}/#{article.created_at.month}/#{article.created_at.day}/slug/#{article.id}" %>
    </li>
  <% end %>
</ul>
